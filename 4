A. lblAddAction → OnSelect

Click the property, Ctrl+A, paste:

Collect(
    colEditActions,
    {
        TempID: GUID(),
        ActionRec: Blank(),
        Descr: "",
        Assignee: Blank(),
        StatusText: "In-Progress",
        Removed: false
    }
)
B. drpStatus → ItemDisplayText

Ctrl+A, Delete. Leave it blank. Don't type anything.

C. drpStatus → Default

Ctrl+A, paste:

ThisItem.StatusText



LookUp(drpStatus.Items, Value = ThisItem.StatusText)



If(
    !ThisItem.InMonth,
    RGBA(250, 251, 252, 1),
    ThisItem.CellDate = Coalesce(varCalSelected, Today()),
    RGBA(242, 248, 255, 1),
    ThisItem.EvtCount > 0,
    RGBA(219, 234, 254, 1),
    ThisItem.IsToday,
    RGBA(247, 251, 255, 1),
    ThisItem.IsWeekend,
    RGBA(253, 253, 254, 1),
    RGBA(255, 255, 255, 1)
)
